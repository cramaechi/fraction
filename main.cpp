//Program uses a class type called Fraction that is used to represent ratios of two integers.
#include "fraction.h"

int main()
{
    Fraction ratio1;
    ratio1.input();
    ratio1.output();

    return 0;
}
